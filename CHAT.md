# OnPad chat shell (?v=62)

Phone-first chat overlay: Global / Local / Lobby (disabled until CH-*) / DM.
Uses `js/chat.js` + CSS in `css/app.css`. Local prefers `OnPadTown.id()` with `town:unknown` fallback.
Does not change World/lobby map layers.
